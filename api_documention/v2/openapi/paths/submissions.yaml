post:
  tags:
    - Submissions
  description: Process submissions
  security:
    - jwt: []
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
    '403':
      description: Not permitted to perform operation
      content:
        application/json:
          schema:
    '422':
      description: Unprocessable Entity
      content:
        application/json:
          schema:
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../schemas/submission_payload.yaml
    required: true
